name: Hello World

on:
  workflow_dispatch:
    inputs:
      name:
        description: "app's name"
        required: true
        type: string
jobs:
  say-hello:
    runs-on: codebuild-quang-test-${{ github.run_id }}-${{ github.run_attempt }}
    steps:
      - name: Print greeting
        run: echo "🚀 Hello, World ${{ inputs.base_url_app }} from GitHub Actions!"
